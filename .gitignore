node_modules/
.worktrees/
.superpowers/
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.local
.next/
.turbo/
dist/

# User data — lives in VLDR_HOME (~/.volundr), not in the repo
projects/
global/
.claude/worktrees/

# Development artifacts (not for published repo)
.mother-claude.bak/
.motherclaude/
.playwright-mcp/
docs/superpowers/
/*.png
.mcp.json
# but DO track vetted, secret-free pack MCP templates (FRW-BL-035) — ${ENV_VAR} placeholders only
!framework/packs/*/.mcp.json
.claude/settings.local.json

# Local-only files (machine-specific, never push)
docker-compose.local.yml
framework/machine-constraints.md

# Runtime artifacts
.claude/scheduled_tasks.lock
__pycache__/
